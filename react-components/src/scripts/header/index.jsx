import React from "react";

// export a react component named Footer
export default class Header extends React.Component {
  // remember a react component must implement the render() method.
  render() {
    // render a h1
    return (
      <h1>
        this is the header
      </h1>
    );
  }
}
